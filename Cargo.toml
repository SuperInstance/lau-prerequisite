[package]
name = "lau-prerequisite"
version = "0.1.0"
edition = "2021"
description = "Prerequisite/dependency graph for PLATO concepts"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
